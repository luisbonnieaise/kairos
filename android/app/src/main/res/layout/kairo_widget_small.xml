<?xml version="1.0" encoding="utf-8"?>
<!-- Home Screen 2x2. 回路 no topo-esquerda + frase do dia (serif, até 4 linhas).
     Toque abre /today (PendingIntent definido no provider). -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/kairo_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/kairo_bg"
    android:padding="16dp">

    <!-- Conteúdo normal -->
    <LinearLayout
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/symbol"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="回路"
                android:textColor="@color/kairo_kin"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/staleness"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="·"
                android:textColor="@color/kairo_smoke"
                android:textSize="12sp"
                android:visibility="gone" />
        </LinearLayout>

        <TextView
            android:id="@+id/phrase"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:fontFamily="serif"
            android:textColor="@color/kairo_body"
            android:textSize="14sp"
            android:lineSpacingExtra="3dp"
            android:maxLines="4"
            android:ellipsize="end" />
    </LinearLayout>

    <!-- Estado vazio (fresh install): 回路 + Kairo centralizados -->
    <LinearLayout
        android:id="@+id/empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="回路"
            android:textColor="@color/kairo_kin"
            android:textSize="20sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="Kairo"
            android:textColor="@color/kairo_ash"
            android:textSize="12sp" />
    </LinearLayout>
</FrameLayout>
